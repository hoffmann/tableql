Implementation of a javascript module to provide JQL like query capabilities.

The spec is in README.md

The javascript code is in tableql.js.

The tests are in tableql.test.js.

The tests can be executed with

    node --test
